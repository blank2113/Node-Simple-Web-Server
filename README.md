# 🚀 Simple HTTP Web Server (Node.js + TypeScript)

Минималистичный, быстрый и отказоустойчивый веб-сервер, написанный на **TypeScript** с использованием встроенного модуля `node:http` без сторонних веб-фреймворков. 

Проект разработан с учетом лучших практик (Senior-style): логика декомпозирована на чистые изолированные функции, внедрена защита от инфраструктурных сбоев, конфигурация управляется через переменные окружения, а раздача статики оптимизирована с помощью потоков (Streams). Сервер полностью готов к масштабированию в режиме кластера через PM2.

---

## 🏗️ Архитектура и жизненный цикл запроса

Архитектура построена на плоском конвейере обработки (Flat Pipeline). Каждая стадия запроса изолирована, что предотвращает падение сервера при некорректных действиях пользователя.

```text
[Клиент] 
   │
   ▼
[Порт :PORT] ──► (Инициализация: проверка EADDRINUSE)
   │
   ▼
[http.createServer] 
   │
   ├──► [parseSafePath] ──────► (Декодирование URL, защита от дефектов)
   │
   ├──► [startsWith] ─────────► (Валидация безопасности Directory Traversal)
   │
   ├──► [resolveStaticFile] ──► (Умный поиск файлов: прямой -> .html -> index.html)
   │
   ▼
[fs.createReadStream] ──► [stream.pipe(res)] ──► [Ответ клиенту]

📖 Техническая документация и исходный кодПроект декомпозирован на изолированные логические блоки. Вы можете перейти к детальному описанию архитектуры или сразу к файлам кода по прямым кликабельным ссылкам:Архитектурные спецификации

📂 Спецификация статического роутера (RESOLVEURL.md) — подробный разбор конвейера валидации входящих URL, паттерна «Массив кандидатов» и защиты от утечки данных.

🏷️ Спецификация карты типов (MIMETYPES.md) — описание поддерживаемых контент-типов, защита от MIME Sniffing и инструкция по безопасной обработке неизвестных расширений.Исходный код проекта (src/)

📄 Точка входа сервера (index.ts) — инициализация http.createServer, прослушивание портов и глобальный перехват инфраструктурных ошибок.📄 Модуль роутинга (resolveUrl.ts) — чистая функция обработки входящих запросов, валидации путей и генерации высокопроизводительных стримов.

📄 Конфигурация типов (type.ts) — экспортируемый словарь MIME_TYPES.📂 Структура проектаФинальная архитектура файлов и каталогов репозитория выглядит следующим образом:Plaintext.

├── dist/                    # 📁 Скомпилированный production-код (генерируется tsc)
├── docs/                    # 📁 Архитектурная документация проекта
│   ├── MIMETYPES.md         # Спецификация карты медиа-типов контента
│   └── RESOLVEURL.md        # Спецификация логики статического роутера
├── src/
│   ├── static/              # 📁 Папка со статическими файлами (HTML, CSS, JS, медиа)
│   │   ├── main/
│   │   │   └── index.html   # Главная страница (дефолтный роут сервера)
│   │   └── about.html       # Пример страницы для Pretty URL (/about)
│   ├── index.ts             # 📄 Точка входа приложения (инициализация http.createServer)
│   ├── resolveUrl.ts        # 📄 Логика обработки и стриминга запросов
│   └── type.ts              # 📄 Экспортируемый объект конфигурации MIME_TYPES
├── .env                     # Локальные переменные окружения (игнорируются Git)
├── .env.example             # Шаблон конфигурации для команды разработки
├── .gitignore               # Исключения и черные списки для Git-репозитория
├── package.json             # Зависимости проекта и npm-скрипты автоматизации
└── tsconfig.json            # Настройки и строгость компилятора TypeScript

⚙️ Конфигурация окружения (.env)Сервер динамически подтягивает настройки через пакет dotenv. Создайте файл .env в корневом каталоге проекта на основе шаблона:Bashcp .env.example .env
Внутри файла задайте порт (по умолчанию сервер использует порт 3000):Фрагмент кодаPORT=3000

🛠️ Скрипты управления (package.json)КомандаОписаниеnpm run buildКомпиляция TypeScript-кода из src/ в JavaScript dist/.npm run watchЗапуск компилятора в режиме реального времени (авто-сборка при изменении файлов).npm startЗапуск production-сборки в один поток через стандартный Node.js.npm run cluster:startProduction: Сборка проекта и запуск кластера PM2 на все доступные ядра CPU.npm run cluster:reloadМягкий перезапуск воркеров по очереди без остановки сервера (Zero-Downtime).npm run cluster:statusМониторинг запущенных инстансов, общего потребления памяти и CPU.npm run cluster:stopПолная остановка сервера и выгрузка процессов из менеджера PM2.